# IPTV M3U 聚合器 Go 重构 - 项目初始化指南

## 📋 概述

本指南说明如何初始化 `go rebuild/` 目录下的 Go 项目，包括目录结构、文件配置和初始化脚本。

## 🏗️ 完整项目结构

```
go rebuild/
└── iptv-aggregator/
    ├── main.go                         # 应用入口
    ├── go.mod                          # Go 模块定义
    ├── go.sum                          # 依赖锁定
    ├── Makefile                        # 构建脚本
    ├── .gitignore                      # Git 忽略规则
    ├── README.md                       # 项目说明
    │
    ├── config/                         # 配置管理
    │   ├── config.go                   # 配置结构和加载
    │   └── config_test.go              # 配置测试
    │
    ├── models/                         # 数据模型
    │   ├── subscription.go             # 订阅源模型
    │   ├── channel.go                  # 频道模型
    │   ├── config.go                   # 配置模型
    │   └── models_test.go              # 模型测试
    │
    ├── services/                       # 业务逻辑
    │   ├── subscription_manager.go     # 订阅管理
    │   ├── subscription_manager_test.go
    │   ├── parser.go                   # M3U 解析
    │   ├── parser_test.go
    │   ├── aggregator.go               # 频道聚合
    │   ├── aggregator_test.go
    │   ├── stream_tester.go            # 流测试
    │   ├── stream_tester_test.go
    │   ├── scheduler.go                # 任务调度
    │   ├── scheduler_test.go
    │   ├── exporter.go                 # 导出功能
    │   ├── exporter_test.go
    │   ├── persistent_cache.go         # 持久化缓存
    │   └── persistent_cache_test.go
    │
    ├── handlers/                       # HTTP 处理器
    │   ├── web.go                      # Web 路由
    │   ├── web_test.go
    │   ├── api.go                      # API 路由
    │   ├── api_test.go
    │   └── middleware.go               # 中间件
    │
    ├── utils/                          # 工具函数
    │   ├── logger.go                   # 日志工具
    │   ├── helpers.go                  # 辅助函数
    │   └── utils_test.go
    │
    ├── static/                         # 静态文件（嵌入式）
    │   ├── css/
    │   │   └── style.css
    │   └── js/
    │       └── main.js
    │
    ├── templates/                      # HTML 模板（嵌入式）
    │   ├── index.html
    │   ├── subscriptions.html
    │   ├── channels.html
    │   ├── export.html
    │   ├── settings.html
    │   └── layout.html
    │
    ├── data/                           # 数据目录（运行时）
    │   ├── subscriptions.json          # 订阅源数据
    │   ├── channels.json               # 频道数据
    │   ├── config.json                 # 配置文件
    │   └── exports/                    # 导出文件目录
    │
    ├── build/                          # 构建输出目录
    │   ├── windows/
    │   │   └── iptv-aggregator.exe
    │   └── debian/
    │       └── iptv-aggregator
    │
    ├── scripts/                        # 构建脚本
    │   ├── build.sh                    # Linux/Mac 构建脚本
    │   ├── build.bat                   # Windows 构建脚本
    │   ├── build-windows.sh            # 跨平台构建 Windows
    │   ├── build-debian.sh             # 跨平台构建 Debian
    │   ├── init.sh                     # 项目初始化脚本
    │   └── init.bat                    # Windows 初始化脚本
    │
    ├── docker/                         # Docker 配置
    │   ├── Dockerfile                  # Debian 容器
    │   ├── Dockerfile.windows          # Windows 容器（可选）
    │   └── docker-compose.yml
    │
    ├── tests/                          # 测试文件
    │   ├── unit/                       # 单元测试
    │   ├── integration/                # 集成测试
    │   └── performance/                # 性能测试
    │
    ├── docs/                           # 文档
    │   ├── README.md                   # 项目说明
    │   ├── INSTALL.md                  # 安装指南
    │   ├── BUILD.md                    # 构建指南
    │   ├── DEPLOY.md                   # 部署指南
    │   ├── API.md                      # API 文档
    │   └── ARCHITECTURE.md             # 架构文档
    │
    ├── .github/                        # GitHub 配置
    │   └── workflows/
    │       ├── build.yml               # 构建工作流
    │       ├── test.yml                # 测试工作流
    │       └── release.yml             # 发布工作流
    │
    └── README.md                       # 项目说明
```

## 📝 初始化步骤

### 步骤 1：创建项目目录

```bash
# 创建项目目录
mkdir -p "go rebuild/iptv-aggregator"
cd "go rebuild/iptv-aggregator"

# 初始化 Git 仓库
git init
```

### 步骤 2：初始化 Go 模块

```bash
# 初始化 Go 模块
go mod init github.com/yourusername/iptv-aggregator

# 创建 go.sum（初始为空）
touch go.sum
```

### 步骤 3：创建目录结构

```bash
# 创建所有必需的目录
mkdir -p config
mkdir -p models
mkdir -p services
mkdir -p handlers
mkdir -p utils
mkdir -p static/css
mkdir -p static/js
mkdir -p templates
mkdir -p data/exports
mkdir -p build/windows
mkdir -p build/debian
mkdir -p scripts
mkdir -p docker
mkdir -p tests/unit
mkdir -p tests/integration
mkdir -p tests/performance
mkdir -p docs
mkdir -p .github/workflows
```

### 步骤 4：创建基础文件

#### main.go

```go
package main

import (
    "flag"
    "fmt"
    "log"
    "os"
    "os/signal"
    "syscall"
    
    "github.com/yourusername/iptv-aggregator/config"
    "github.com/yourusername/iptv-aggregator/handlers"
    "github.com/yourusername/iptv-aggregator/services"
    "github.com/yourusername/iptv-aggregator/utils"
)

var (
    Version   = "dev"
    BuildTime = "unknown"
    GitCommit = "unknown"
)

func main() {
    // 命令行标志
    configPath := flag.String("config", "config.json", "Configuration file path")
    port := flag.Int("port", 8080, "Server port")
    version := flag.Bool("version", false, "Show version")
    help := flag.Bool("help", false, "Show help")
    
    flag.Parse()
    
    if *help {
        flag.PrintDefaults()
        os.Exit(0)
    }
    
    if *version {
        fmt.Printf("IPTV M3U Aggregator\n")
        fmt.Printf("Version: %s\n", Version)
        fmt.Printf("Build Time: %s\n", BuildTime)
        fmt.Printf("Git Commit: %s\n", GitCommit)
        os.Exit(0)
    }
    
    // 初始化日志
    logger := utils.NewLogger()
    logger.Info("Starting IPTV M3U Aggregator...")
    
    // 加载配置
    cfg, err := config.LoadConfig(*configPath)
    if err != nil {
        logger.Error("Failed to load config: %v", err)
        os.Exit(1)
    }
    
    // 覆盖端口
    if *port != 8080 {
        cfg.Port = *port
    }
    
    // 初始化服务
    subscriptionMgr := services.NewSubscriptionManager(cfg.DataDir)
    parser := services.NewM3UParser(cfg.RequestTimeout)
    aggregator := services.NewChannelAggregator(cfg.DataDir)
    tester := services.NewStreamTester(cfg.StreamTestTimeout, cfg.MaxTestWorkers)
    scheduler := services.NewScheduler()
    exporter := services.NewChannelExporter(cfg.DataDir)
    
    // 初始化 HTTP 服务器
    router := handlers.SetupRouter(
        subscriptionMgr,
        parser,
        aggregator,
        tester,
        scheduler,
        exporter,
        cfg,
    )
    
    // 启动服务器
    go func() {
        addr := fmt.Sprintf("%s:%d", cfg.Host, cfg.Port)
        logger.Info("Server listening on %s", addr)
        if err := router.Run(addr); err != nil {
            logger.Error("Server error: %v", err)
        }
    }()
    
    // 优雅关闭
    sigChan := make(chan os.Signal, 1)
    signal.Notify(sigChan, syscall.SIGINT, syscall.SIGTERM)
    
    <-sigChan
    logger.Info("Shutting down...")
    scheduler.Stop()
    logger.Info("Goodbye!")
}
```

#### go.mod

```
module github.com/yourusername/iptv-aggregator

go 1.21

require (
    github.com/gin-gonic/gin v1.9.1
    github.com/robfig/cron/v3 v3.0.1
    github.com/texttheater/golang-levenshtein/levenshtein v0.0.0-20200805054039-cae8b0eaed6c
    github.com/grafov/m3u8 v0.11.1
    github.com/prometheus/client_golang v1.17.0
)
```

#### .gitignore

```
# Binaries
*.exe
*.exe~
*.dll
*.so
*.so.*
*.dylib

# Test binary
*.test

# Output of coverage
*.out
coverage.html

# Go workspace file
go.work

# Build output
/build/
/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data files
/data/subscriptions.json
/data/channels.json
/data/exports/

# Logs
*.log

# Temporary files
*.tmp
*.bak
```

#### Makefile

```makefile
.PHONY: help build build-windows build-debian clean test

BINARY_NAME=iptv-aggregator
VERSION=$(shell git describe --tags --always --dirty)
BUILD_TIME=$(shell date -u '+%Y-%m-%d %H:%M:%S')
GIT_COMMIT=$(shell git rev-parse --short HEAD)

WINDOWS_OUTPUT=build/windows/$(BINARY_NAME).exe
DEBIAN_OUTPUT=build/debian/$(BINARY_NAME)

LDFLAGS=-ldflags "-X main.Version=$(VERSION) -X main.BuildTime=$(BUILD_TIME) -X main.GitCommit=$(GIT_COMMIT)"

help:
	@echo "IPTV M3U Aggregator - Build Commands"
	@echo "make build              - Build for current platform"
	@echo "make build-windows      - Build for Windows x64"
	@echo "make build-debian       - Build for Debian/Linux"
	@echo "make build-all          - Build for all platforms"
	@echo "make clean              - Clean build artifacts"
	@echo "make test               - Run tests"

build:
	go build $(LDFLAGS) -o $(BINARY_NAME) .

build-windows:
	mkdir -p build/windows
	CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build $(LDFLAGS) -o $(WINDOWS_OUTPUT) .

build-debian:
	mkdir -p build/debian
	CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build $(LDFLAGS) -o $(DEBIAN_OUTPUT) .
	chmod +x $(DEBIAN_OUTPUT)

build-all: build-windows build-debian

clean:
	rm -rf build/
	rm -f $(BINARY_NAME) $(BINARY_NAME).exe

test:
	go test -v ./...

test-race:
	go test -race -v ./...
```

#### README.md

```markdown
# IPTV M3U Aggregator - Go Version

IPTV M3U 聚合器的 Go 重构版本，提供更好的性能和并发处理。

## 功能

- 聚合多个 IPTV M3U 订阅源
- 频道去重和合并
- 流可用性测试
- Web UI 管理界面
- RESTful API
- 定时任务调度
- 数据导出（M3U/JSON）

## 系统要求

- Go 1.21+
- 4GB RAM（推荐）
- 100MB 磁盘空间

## 快速开始

### 构建

```bash
# 构建当前平台
make build

# 构建 Windows x64
make build-windows

# 构建 Debian/Linux x64
make build-debian

# 构建所有平台
make build-all
```

### 运行

```bash
# 直接运行
./iptv-aggregator

# 指定配置文件
./iptv-aggregator -config config.json

# 指定端口
./iptv-aggregator -port 8080

# 显示版本
./iptv-aggregator -version
```

### 访问

打开浏览器访问 `http://localhost:8080`

## 文档

- [安装指南](docs/INSTALL.md)
- [构建指南](docs/BUILD.md)
- [部署指南](docs/DEPLOY.md)
- [API 文档](docs/API.md)
- [架构文档](docs/ARCHITECTURE.md)

## 许可证

MIT License
```

### 步骤 5：创建初始化脚本

#### init.sh（Linux/Mac）

```bash
#!/bin/bash

set -e

echo "Initializing IPTV M3U Aggregator project..."

# 创建目录结构
echo "Creating directory structure..."
mkdir -p config models services handlers utils
mkdir -p static/css static/js
mkdir -p templates
mkdir -p data/exports
mkdir -p build/windows build/debian
mkdir -p scripts docker
mkdir -p tests/unit tests/integration tests/performance
mkdir -p docs .github/workflows

# 初始化 Git
if [ ! -d .git ]; then
    echo "Initializing Git repository..."
    git init
fi

# 初始化 Go 模块
if [ ! -f go.mod ]; then
    echo "Initializing Go module..."
    go mod init github.com/yourusername/iptv-aggregator
fi

# 下载依赖
echo "Downloading dependencies..."
go mod download

# 创建基础文件
echo "Creating base files..."
touch config/.gitkeep
touch models/.gitkeep
touch services/.gitkeep
touch handlers/.gitkeep
touch utils/.gitkeep
touch tests/unit/.gitkeep
touch tests/integration/.gitkeep
touch tests/performance/.gitkeep
touch docs/.gitkeep

echo "✓ Project initialized successfully!"
echo ""
echo "Next steps:"
echo "1. Update go.mod with your module name"
echo "2. Create main.go and other source files"
echo "3. Run 'go mod tidy' to update dependencies"
echo "4. Run 'make build' to build the project"
```

#### init.bat（Windows）

```batch
@echo off
setlocal enabledelayedexpansion

echo Initializing IPTV M3U Aggregator project...

REM 创建目录结构
echo Creating directory structure...
if not exist config mkdir config
if not exist models mkdir models
if not exist services mkdir services
if not exist handlers mkdir handlers
if not exist utils mkdir utils
if not exist static\css mkdir static\css
if not exist static\js mkdir static\js
if not exist templates mkdir templates
if not exist data\exports mkdir data\exports
if not exist build\windows mkdir build\windows
if not exist build\debian mkdir build\debian
if not exist scripts mkdir scripts
if not exist docker mkdir docker
if not exist tests\unit mkdir tests\unit
if not exist tests\integration mkdir tests\integration
if not exist tests\performance mkdir tests\performance
if not exist docs mkdir docs
if not exist .github\workflows mkdir .github\workflows

REM 初始化 Git
if not exist .git (
    echo Initializing Git repository...
    git init
)

REM 初始化 Go 模块
if not exist go.mod (
    echo Initializing Go module...
    go mod init github.com/yourusername/iptv-aggregator
)

REM 下载依赖
echo Downloading dependencies...
go mod download

echo Project initialized successfully!
echo.
echo Next steps:
echo 1. Update go.mod with your module name
echo 2. Create main.go and other source files
echo 3. Run 'go mod tidy' to update dependencies
echo 4. Run 'make build' to build the project
```

### 步骤 6：运行初始化脚本

```bash
# Linux/Mac
chmod +x scripts/init.sh
./scripts/init.sh

# Windows
scripts\init.bat
```

## 📦 依赖管理

### 添加依赖

```bash
# 添加 Gin 框架
go get github.com/gin-gonic/gin

# 添加任务调度
go get github.com/robfig/cron/v3

# 添加字符串匹配
go get github.com/texttheater/golang-levenshtein/levenshtein

# 添加 HLS 解析
go get github.com/grafov/m3u8

# 添加 Prometheus
go get github.com/prometheus/client_golang

# 整理依赖
go mod tidy
```

### 更新依赖

```bash
# 更新所有依赖
go get -u ./...

# 整理依赖
go mod tidy

# 验证依赖
go mod verify
```

## 🧪 项目验证

### 验证项目结构

```bash
# 检查所有必需的目录
ls -la config models services handlers utils static templates data build scripts docker tests docs

# 检查 Go 模块
cat go.mod
```

### 验证依赖

```bash
# 列出所有依赖
go list -m all

# 检查依赖是否可用
go mod verify
```

### 验证构建

```bash
# 检查代码是否可以编译
go build -v .

# 运行测试
go test ./...
```

## 📋 初始化检查清单

- [ ] 项目目录已创建
- [ ] Git 仓库已初始化
- [ ] Go 模块已初始化
- [ ] 目录结构已创建
- [ ] 基础文件已创建
- [ ] 依赖已下载
- [ ] 项目可以编译
- [ ] 测试可以运行

## 🚀 后续步骤

1. **创建源代码文件**
   - 实现 config/config.go
   - 实现 models/ 中的数据模型
   - 实现 services/ 中的业务逻辑
   - 实现 handlers/ 中的 HTTP 处理器

2. **编写测试**
   - 为每个模块编写单元测试
   - 编写集成测试
   - 编写性能测试

3. **构建和测试**
   - 运行 `make test` 验证代码
   - 运行 `make build-all` 构建二进制
   - 测试 Windows 和 Debian 二进制

4. **部署**
   - 按照构建和部署指南部署应用
   - 配置监控和告警
   - 准备生产环境

---

**最后更新**：2024-01-15
**文档状态**：✅ 完成
**准备就绪**：✅ 可以开始初始化项目
